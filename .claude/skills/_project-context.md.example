<!--
  _project-context.md — consumer-side extension point for pelaggio.

  Copy this file to `_project-context.md` in the same directory and fill in
  the sections below. The three review-heavy skills (`plan`, `shakedown`,
  `ship`) read it via `!cat .claude/skills/_project-context.md 2>/dev/null`,
  so an absent file is silently ignored. Upstream `pelaggio sync`
  never touches either `_project-context.md` or this `.example` template —
  the underscore-prefix exclusion in `planSync()` + the `ALLOWED_DEST` regex
  in `applyAction()` guarantee it.

  Keep content terse and load-bearing. Generic advice dilutes the rubric.
-->

## Data model invariants

<!-- Shape constraints, required relationships, or fields that must never be
     null/empty. What would break if violated? Point to the enforcing code. -->

## Shared components

<!-- Reusable UI / backend components that new work should prefer over
     re-rolling. Name the module path and the "use this when..." trigger. -->

## Domain conventions

<!-- Non-obvious patterns specific to this product: naming, error shapes,
     auth/permission idioms, feature-flag usage, etc. -->

## Merge-conflict resolution patterns

<!-- Recurring conflict shapes and how this team resolves them. Lockfiles,
     generated files, migration numbering, etc. -->
