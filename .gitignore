node_modules
packages/*/node_modules/
.dev/
.claude/worktrees/
.claude/settings.local.json
.claude/shipwreck.log
*.log
# Node diagnostic/crash reports (report.<ts>.<pid>.<seq>.json) that node writes to
# cwd on a fatal error — test runs during a cycle drop these and git add -A sweeps
# them into the branch, then chokes the ship step. Ignore so cycles stay clean.
report.*.json
.DS_Store
report.*.json
lefthook-local.yml
packages/web/dist/
packages/web/.astro/
packages/web/public/icon-*.png
packages/web/public/apple-touch-icon.png
