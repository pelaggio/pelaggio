name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    # GitHub-hosted (not self-hosted): CI runs on untrusted fork PRs once this
    # repo is public, so it must not execute on our own infrastructure.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5.0.0

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: '24'
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile
      - run: pnpm -r test
      - run: pnpm test:ci
      - run: pnpm --filter @pelaggio/web check:types
      - run: pnpm --filter @pelaggio/web build
      - run: pnpm check
      - run: pnpm check:skills
      - run: pnpm check:trust
      - run: pnpm check:links
